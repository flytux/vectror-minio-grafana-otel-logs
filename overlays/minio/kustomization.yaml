apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - minio.yaml

helmGlobals:
  chartHome: ../../charts    
helmCharts:
  - name: vector
    version: 0.56.0    
    releaseName: vector-aggregator
    valuesFile: values.yaml
    includeCRDs: true
